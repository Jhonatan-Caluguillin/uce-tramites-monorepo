{
  "name": "notifications-service",
  "projectType": "application",
  "sourceRoot": "apps/notifications-service/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -t notifications-service ."
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "python src/main.py"
      }
    }
  }
}