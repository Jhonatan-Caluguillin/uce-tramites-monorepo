{
  "name": "documents-service",
  "projectType": "application",
  "sourceRoot": "apps/documents-service/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "docker build -t documents-service ."
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "python src/main.py"
      }
    }
  }
}